{
  "name": "school-course-fsapp",
  "version": "1.0.0",
  "description": "**Full Stack React App**",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "cd api && npm install && npm start",
    "heroku-postbuild": "cd client && npm install && npm run build",
    "migrate": "sequelize db:migrate"

  },
  "engines": {
    "node": "14.16.0"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "sequelize": "^6.17.0",
    "sequelize-cli": "^6.4.1"
  }
}
